# Run: 2026-07-16 (post-recipes)

**Result: PASS** — zero hard fails, fully on-system output. 5 residual guesses logged (down
from 8 on 2026-07-15).

Same task, same fresh-agent restriction, run after the 07-15 guess-list was encoded as
"Compositions" recipes in `packages/react/llms.txt`. **5 of the 8 baseline guesses closed**:
form-field composition, stack/gap container pairing, native prop pass-through, and size
selection were no longer guesses — the agent *cited* the documented rules where the previous
run had guessed — and the semantic-token-roster gap closed too. Zero invented props again. The
5 residual guesses, all narrower and shallower than the baseline set: (1) programmatic
`data-ds-theme` mechanics — the agent guessed a `useEffect` setting `document.documentElement`,
and separately guessed whether root theming paints `<body>`; (2) field gap *numbers* — the
recipe named label→control and field→field spacing conceptually but not the actual token
values, so the specific gaps were guessed; (3) Tag-as-badge mapping — no documented mapping from
"badge use" to a Tag variant, plus whether a Tag counts against "one accent per visual group"
was unsettled; (4) CSS import order — unstated whether the four token stylesheets must load in
a specific order; (5) content-measure width — no content-measure token existed, so the agent
hardcoded a `maxWidth` value. These 5 topics are the target for the next docs pass; the cycle
exit condition is a third run reporting zero guesses on them.
